function fullBackup(){return {app:'Rutas Maria Jesus',version:1,exportedAt:nowISO(),deviceId:getDeviceId(),config:getConfig(),routes:getRoutes(),sync:readJSON(STORE.SYNC,{})};}
function downloadJSON(){const blob=new Blob([JSON.stringify(fullBackup(),null,2)],{type:'application/json'}); const a=document.createElement('a'); a.href=URL.createObjectURL(blob); a.download='RUTAS_MARIA_JESUS_COPIA_'+new Date().toISOString().slice(0,10)+'.json'; a.click(); URL.revokeObjectURL(a.href);}
function copyJSON(){navigator.clipboard.writeText(JSON.stringify(fullBackup(),null,2)).then(()=>alert('JSON copiado al portapapeles.'));}
function restoreBackup(obj){if(!obj || !Array.isArray(obj.routes)){alert('JSON no válido.');return;} if(!confirm('Esto mezclará/restaurará las rutas de la copia. ¿Continuar?'))return; saveRoutes(mergeRoutes(getRoutes(), obj.routes)); if(obj.config){saveConfig(obj.config); loadConfigForm();} alert('Copia restaurada correctamente.');}
function restoreFromText(){try{restoreBackup(JSON.parse(pasteJsonArea.value));}catch(e){alert('No se ha podido leer el JSON.');}}
function importFile(file){const reader=new FileReader(); reader.onload=()=>{try{restoreBackup(JSON.parse(reader.result));}catch(e){alert('Archivo JSON no válido.');}}; reader.readAsText(file);}
